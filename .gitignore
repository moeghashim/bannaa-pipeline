node_modules/
.next/
dist/
packages/*/node_modules/
packages/*/dist/
apps/*/node_modules/
*.log
*.tsbuildinfo
.DS_Store

.env

.vscode/
.zed/
.idea/
*.swp
*.swo
*~

.env.local

# Claude Code agent metadata (worktree state, session scratch).
# These live in-repo so worktrees can share them, but they shouldn't
# be versioned — each developer's machine has its own agent state.
.claude/
